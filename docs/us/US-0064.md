---
id: US-0064
title: Store browser e roteamento tauriInvoke
epic: EPIC-20
version: v6
status: ✅
moscow: Must
depends_on: []
ready: true
done_when: "Fora do Tauri, services agile invocam BrowserAgileStore com localStorage; fluxo projeto→hub→quadro funciona em vite preview sem erro de backend."
tests: required
tests_status: done
---

# US-0064 — Store browser e roteamento tauriInvoke

**As** visitante da demo web,
**I want** usar a mesma UI agile no navegador com dados que persistem só no meu dispositivo,
**so that** experimente o produto sem instalar Tauri nem SQLite.

## Intent

### Acceptance

- [x] `tauriInvoke` delega para `@tauri-apps/api` no runtime Tauri e para `BrowserAgileStore.invoke` quando `isTauriRuntime()` é falso — services agile não duplicam ramificação.
- [x] `BrowserAgileStore` persiste estado em `localStorage` (chave versionada) e implementa handlers para commands usados por `projectService`, `versionService`, `epicService`, `sprintService`, `storyService` e `workflowService` (incl. `version_epics`, move/reorder, acceptance, dependencies).
- [x] `TauriGuard` não bloqueia render no browser — app carrega normalmente (banner fica em US-0065).
- [x] `pnpm exec tsc --noEmit` e `pnpm build` passam; `pnpm preview` permite criar projeto, versão, épico, sprint e US; reload mantém dados no mesmo browser.

### Why

Hoje a UI só funciona com invoke Rust — abrir o bundle Vite no browser falha na primeira chamada. Interessados em Meridian precisam clonar e rodar Tauri. Este slice entrega um adaptador TypeScript espelhando o contrato dos commands agile, com persistência local-only, reutilizando toda a camada React existente.

### Where

v6-S1 / EPIC-20 / v6. Sem dependências — desbloqueia US-0065 (banner + Pages). Não inclui CI nem ajuste de `base` Vite.

## Plan

### Approach

- Centralizar roteamento em `src/lib/tauriInvoke.ts` usando `isTauriRuntime()` — único ponto DRY entre desktop e demo (SRP: services continuam só declarando commands).
- Implementar `BrowserAgileStore` em `src/features/agile/browser/browserAgileStore.ts` com modelo in-memory + serialização JSON; espelhar regras já validadas no domain (ex. relações story) onde o Rust já valida, para evitar dados incoerentes na demo.
- Exportar `clearBrowserAgileStore()` para reset local (consumido na US-0065); chave `meridian-agile-browser-v1` permite evolução futura de schema.
- Manter `TauriGuard` como wrapper neutro — remover gate que impedia montar rotas no browser.

### Architecture refs

- `docs/05_architecture.md` — § Camadas e limites
- `docs/architecture/data-model.md` — § Modelo agile

### API / DB impact

- _n/a_ no SQLite — adaptador browser substitui invoke apenas fora do Tauri.

### Security notes

- Dados demo ficam em `localStorage` no cliente; sem secrets — alinhado a `02_security.md` para ambiente demo público.

### Related decisions

- `docs/decisions/2026-07-17.json` — v6 demo web localStorage

### Planned

- [x] **manual** — 1. `pnpm build && pnpm preview`. 2. Criar projeto e hierarquia agile completa. 3. Recarregar — dados permanecem. 4. Abrir aba anônima — estado vazio.
- [x] **build** — `pnpm exec tsc --noEmit` e `pnpm build` exit 0.

## Record

### Files

- `src/lib/tauriInvoke.ts`
- `src/lib/isTauriRuntime.ts`
- `src/features/agile/browser/browserAgileStore.ts`
- `src/app/TauriGuard.tsx`

### Backend

- _n/a_

### Frontend

- Roteamento único `tauriInvoke` para Tauri vs browser.
- Store local com handlers para todos os commands agile usados pela UI.
- App renderiza no browser sem gate de backend.

### Scripts / Docs

- _n/a_

### Executed

- `pnpm exec tsc --noEmit` (ok)
- `pnpm build` (ok)
- **suggested commit:** `feat(v6): store browser e tauriInvoke localStorage (US-0064)`
- **git commit:** `60b2257` — feat(v6): demo web localStorage e GitHub Pages (US-0064, US-0065)

## Boundaries

### Out of scope for this story

- Banner “demo web” e botão limpar — US-0065.
- GitHub Pages, `VITE_BASE_PATH`, workflow CI — US-0065.
- Sync com `docs/us/*.md` ou SQLite desktop.

### Notes

Paridade de commands: desconhecido lança erro explícito — facilita detectar gap ao adicionar feature agile.
