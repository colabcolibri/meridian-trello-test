---
id: US-0001
title: Scaffold Tauri Vite React TypeScript
epic: EPIC-01
version: v1
status: ✅
moscow: Must
depends_on: []
ready: true
done_when: "pnpm tauri dev abre janela do app com React montado e scripts documentados no package.json."
tests: required
tests_status: done
---

# US-0001 — Scaffold Tauri Vite React TypeScript

**As** Usuário local,
**I want** que o projeto base compile e abra uma janela desktop,
**so that** o time possa implementar features sobre fundação estável.

## Intent

### Acceptance

- [x] Repositório contém app Tauri 2 + Vite + React + TypeScript com `pnpm tauri dev` funcional.
- [x] Estrutura de pastas segue camadas em `04_principles.md` (`src/app`, `src/domain`, `src/features`, `src-tauri`).
- [x] ESLint/TypeScript strict configurados; `pnpm lint` e `pnpm build` (frontend) executam sem erro.

### Why

Sem scaffold, nenhuma US posterior pode rodar código real. Esta slice entrega apenas o esqueleto executável — janela aberta, hot reload, comandos npm — sem persistência nem UI kanban.

### Where

Primeira US da v1 / EPIC-01 / sprint v1-S1. Sem dependências. Desbloqueia US-0002 (migrations) e toda a cadeia de produto.

## Plan

### Approach

- Inicializar monorepo Tauri 2 + Vite + React + TypeScript na raiz com `pnpm create tauri-app` (template react-ts) e fixar versões no `package.json` conforme `01_tech_stack.md`, para que US-0002 já tenha `src-tauri/` estável.
- Criar esqueleto de camadas vazio (`src/app/`, `src/domain/`, `src/features/`, `src/components/`) com `App.tsx` placeholder em `src/app/` — evita retrabalho de paths nas US seguintes e cumpre SRP desde o dia zero.
- Configurar Tailwind CSS v4, TypeScript `strict`, ESLint e scripts `dev`, `build`, `lint`, `tauri dev`, `tauri build` no `package.json`, registrando a escolha Tailwind nas Notes ao fechar.

### Architecture refs

- `docs/05_architecture.md` — § Camadas e limites
- `docs/01_tech_stack.md` — Frontend, Desktop e persistência
- `docs/08_environments.md` — Desenvolvimento local

### API / DB impact

- _n/a_ — nenhuma migration nem command nesta US.

### Security notes

- _n/a_ — sem rede nem secrets; `.gitignore` já protege `.env`.

### Related decisions

- `docs/decisions/2026-07-02.json` — Stack Tauri + SQLite

### Planned

- [x] **manual** — 1. `pnpm install`. 2. `pnpm tauri dev`. 3. Janela desktop abre com texto placeholder React. 4. Confirmar pastas `src/app`, `src/domain`, `src/features`, `src/components`, `src-tauri`.
- [x] **automated** — `pnpm lint` exit 0.
- [x] **automated** — `pnpm build` exit 0.

## Record

### Files

- `package.json` — scripts dev/build/lint/tauri; deps React 19 + Tauri 2 + Tailwind v4
- `vite.config.ts` — plugins React + Tailwind; porta 1420 para Tauri
- `eslint.config.js` — ESLint 9 flat config + typescript-eslint
- `index.html` — entry `/src/app/main.tsx`
- `src/app/main.tsx`, `src/app/App.tsx`, `src/app/styles.css` — shell placeholder com Tailwind
- `src/domain/index.ts` — stub de domínio
- `src/features/.gitkeep`, `src/components/.gitkeep` — camadas reservadas
- `src-tauri/` — projeto Tauri renomeado para `kanban-local` (Cargo.toml, tauri.conf.json, icons)

### Backend

- `src-tauri/Cargo.toml` — crate `kanban-local`; deps tauri 2 + opener plugin
- `src-tauri/src/main.rs` — entry `kanban_local_lib::run()`
- `src-tauri/src/lib.rs` — builder Tauri mínimo (DB wiring adicionado em US-0002)

### Frontend

- React 19 + TypeScript strict + Tailwind CSS v4 via `@tailwindcss/vite`.
- Placeholder "Kanban local — workspace em construção" centralizado.

### Scripts / Docs

- _n/a_

### Executed

- `pnpm install` — 190 packages, esbuild approved
- `pnpm lint` — exit 0
- `pnpm build` — exit 0 (dist gerado)
- **suggested commit:** `feat(scaffold): add Tauri 2 + Vite + React + TypeScript foundation (US-0001)`

## Boundaries

### Out of scope for this story

- Schema SQLite, commands de domínio, telas kanban.

### Notes

Tailwind CSS v4 adotado (via `@tailwindcss/vite`).
