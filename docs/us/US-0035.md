---
id: US-0035
title: Commands CRUD user stories e acceptance
epic: EPIC-11
version: v3
status: ✅
moscow: Must
depends_on: [US-0032, US-0034]
ready: true
done_when: "CRUD US com acceptance e depends_on; move_story entre colunas workflow persiste order."
tests: required
tests_status: done
---

# US-0035 — Commands CRUD user stories e acceptance

**As** Usuário local,
**I want** cadastrar user stories com critérios de aceite e dependências,
**so that** meu backlog reflita a estrutura Meridian no SQLite.

## Intent

### Acceptance

- [x] Commands: `list_stories`, `create_story`, `update_story`, `delete_story`, `set_story_acceptance`, `set_story_dependencies`, `move_story` (column_id + order_index).
- [x] Story inclui campos Intent/Plan mínimos: why, where_text, done_when, moscow, ready, status, approach opcional.
- [x] Acceptance criteria como linhas ordenadas com text e checked (0/1).
- [x] `depends_on` como lista de story ids; validação impede ciclo simples (A→B→A).
- [x] Gerador de próximo id US-XXXX por projeto.

### Why

Esta slice completa a API agile: o quadro e o modal só consomem commands — não SQL direto na UI. Sem move_story transacional, o DnD v2 não se aplica ao backlog. Depende de versão/epic/sprint existentes para FKs.

### Where

v3-S1 / EPIC-11. Depende de US-0032 e US-0034. Desbloqueia US-0038 (board), US-0041/0042 (modal).

## Plan

### Approach

- Implementar `stories.rs` com list/get/create/update, acceptance, dependencies, move/reorder — validação de ciclo e status 🔶/✅ no backend.
- `storyService.ts` concentra invoke; domain types em `agileTypes.ts`.

### Architecture refs

- `docs/05_architecture.md` — § Fluxo principal

### API / DB impact

- Tabelas `user_stories`, `acceptance_criteria`, `story_dependencies`; commands Rust.

### Security notes

- _n/a_

### Related decisions

- _n/a_

### Planned

- [x] **manual** — 1. Criar duas US; B depends_on A. 2. Adicionar 3 acceptance em A. 3. move_story entre colunas. 4. Reload — estado idêntico.
- [x] **automated** — testes Rust unitários em validação de ciclo de dependência (se aplicável).

## Record

### Files

- `src-tauri/src/commands/agile/stories.rs`
- `src-tauri/src/models.rs`
- `src/features/agile/storyService.ts`

### Backend

API completa de user stories + acceptance + move_story.

### Frontend

storyService.

### Scripts / Docs

_n/a_

### Executed

- `pnpm lint` — exit 0 (warnings only)
- `pnpm build` — exit 0
- `cargo test` — exit 0
- **suggested commit:** `feat(agile): user story CRUD and move (US-0035)`

## Boundaries

### Out of scope for this story

- UI board/modal — v3-S3/S4.
- Record / Executed — preenchido só no fluxo docs ou US futura.
- Sync board.json.

### Notes

Status 🔶 exige campo `missing_note` ou texto em acceptance — validar no update_story.
