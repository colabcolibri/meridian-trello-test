---
id: US-0017
title: Prioridade observações e datas read-only
epic: EPIC-05
version: v1
status: ✅
moscow: Must
depends_on: [US-0013]
ready: true
done_when: "Modal edita prioridade e observações; exibe created_at e updated_at read-only."
tests: required
tests_status: done
---

# US-0017 — Prioridade observações e datas read-only

**As** Usuário local,
**I want** definir prioridade e notas pessoais no cartão,
**so that** eu registre urgência e contexto extra sem colaboração.

## Intent

### Acceptance

- [x] Select prioridade: baixa, média, alta ou vazio.
- [x] Campo observações (texto livre, sem histórico de versões).
- [x] Exibir created_at e updated_at formatados, não editáveis.
- [x] Badge prioridade visível no compacto quando definida.

### Why

Completa metadados da spec de cartão. Observações substituem comentários colaborativos — uma nota estática por cartão.

### Where

EPIC-05, v1-S5. Depende de US-0013. Fecha metadados antes de DnD final.

## Plan

### Approach

- Adicionar campos `priority` e `notes` ao fluxo de `update_card` já usado pelo modal; constantes `PRIORITY_LABELS` em `src/domain/constants.ts` mapeando low/medium/high para pt-BR.
- Componentes `PrioritySelect` e `NotesTextarea` dentro do modal; seção read-only `MetadataDates` exibindo `created_at` e `updated_at` formatados via `dates.ts`.
- Badge de prioridade no `CardCompact` reutilizando cores de `constants.ts` — mesma paleta modal/coluna.

### Architecture refs

- `docs/05_architecture.md` — § Modelo de dados
- `docs/architecture/data-model.md` — cards (priority, notes)

### API / DB impact

- UPDATE `cards.priority`, `cards.notes`, `updated_at` automático.

### Security notes

- Notes texto plano; limite sugerido 10k chars validado no domain.

### Related decisions

- _n/a_

### Planned

- [x] **manual** — 1. Set prioridade "Alta" — badge no compacto. 2. Escrever observação longa — persiste. 3. Datas de criação/atualização visíveis e não editáveis. 4. Reload — todos campos ok.
- [x] **automated** — `pnpm test src/domain/constants.test.ts` se houver map de prioridade; senão `pnpm lint` exit 0.

## Record

### Files

- `src/features/cards/ui/CardDetailModal.tsx`
- `src/domain/constants.ts`

### Backend

update_card priority/notes.

### Frontend

Select prioridade, observações, datas read-only.

### Scripts / Docs

_n/a_

### Executed

- pnpm build exit 0.
- **suggested commit:** `feat(cards): priority notes metadata (US-0017)`


## Boundaries

### Out of scope for this story

- Thread de comentários, @menções.

### Notes

_n/a_
