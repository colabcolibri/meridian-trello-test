---
id: US-0038
title: Quadro kanban colunas workflow agile
epic: EPIC-13
version: v3
status: ✅
moscow: Must
depends_on: [US-0035, US-0037]
ready: true
done_when: "Board mostra colunas workflow padrão; DnD move US; ordem persiste via move_story."
tests: required
tests_status: done
---

# US-0038 — Quadro kanban com colunas de workflow agile

**As** Usuário local,
**I want** ver as user stories do sprint em colunas de workflow e arrastá-las,
**so that** eu acompanhe progresso agile visualmente.

## Intent

### Acceptance

- [x] Colunas padrão seed por projeto/sprint: Backlog, Pronta, Em progresso, Revisão, Concluída (editável fica out of scope).
- [x] `list_stories` filtrado por sprint_id popula colunas por `workflow_column_id`.
- [x] DnD intra e inter-coluna chama `move_story`; sem clone visual (regressão v2).
- [x] Coluna Concluída sincroniza status US para ✅ quando movida (regra documentada).
- [x] Empty state quando sprint sem stories.

### Why

Este é o coração visível da v3: transforma "cards de tarefa" em "user stories agile". Reutiliza BoardView/DnD v2 com novo domínio. Sozinha, ainda não mostra badges ricos — US-0039 incrementa o cartão.

### Where

v3-S3 / EPIC-13. Depende de US-0035 (API) e US-0037 (contexto sprint). Desbloqueia US-0039, US-0040, US-0041.

## Plan

### Approach

- `AgileBoardView.tsx` adapta padrão DnD de `BoardView.tsx` para `storyService.move/reorder` — placeholder anti-clone preservado.
- `workflowService` carrega colunas seed; stories agrupadas por `workflow_column_id`.

### Architecture refs

- `docs/architecture/ui-kanban.md` — § Drag and drop
- `docs/05_architecture.md` — § Fluxo principal

### API / DB impact

- _n/a_ — consome `move_story`, `list_stories`.

### Security notes

- _n/a_

### Related decisions

- _n/a_

### Planned

- [x] **manual** — 1. Sprint com 3 US. 2. Arrastar Backlog → Em progresso. 3. Reload — posição correta. 4. Sem duplicata visual.
- [x] **automated** — `pnpm tauri dev` smoke; `pnpm lint` exit 0.

## Record

### Files

- `src/features/agile/ui/AgileBoardView.tsx`
- `src/features/agile/workflowService.ts`
- `src-tauri/src/commands/agile/workflow.rs`

### Backend

list_workflow_columns.

### Frontend

Board agile 5 colunas + DnD US.

### Scripts / Docs

_n/a_

### Executed

- `pnpm lint` — exit 0 (warnings only)
- `pnpm build` — exit 0
- `cargo test` — exit 0
- **suggested commit:** `feat(agile): workflow kanban board (US-0038)`

## Boundaries

### Out of scope for this story

- Badges épico/MoSCoW — US-0039.
- Filtros — US-0040.
- Modal detalhe — US-0041.
- Editar nomes de colunas workflow.

### Notes

Mapear coluna → status parcialmente; status ✅ pode ser derivado da coluna Concluída.
