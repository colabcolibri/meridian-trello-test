---
id: US-0009
title: Criar cartões com título obrigatório
epic: EPIC-04
version: v1
status: ✅
moscow: Must
depends_on: [US-0008]
ready: true
done_when: "Adicionar cartão em coluna exige título; cartão persiste e aparece na lista."
tests: required
tests_status: done
---

# US-0009 — Criar cartões com título obrigatório

**As** Usuário local,
**I want** adicionar tarefas rapidamente em uma coluna,
**so that** eu capture trabalho no estágio correto do fluxo.

## Intent

### Acceptance

- [x] Botão/ação "Adicionar cartão" por coluna abre input; submit sem título bloqueado.
- [x] Cartão criado aparece no fim da coluna com título visível.
- [x] Dados persistem; reload mantém cartões.

### Why

Cartões são unidade de trabalho. Primeira entrega de EPIC-04 — valor mínimo kanban (tarefa na coluna).

### Where

v1-S4 / EPIC-04. Depende de US-0008. Desbloqueia US-0010, US-0011, US-0012.

## Plan

### Approach

- Adicionar `validateCardTitle` em `src/domain/validators.ts` (reutilizar padrão de board name) e command `create_card` em `src-tauri/src/commands/cards.rs` gravando `created_at`/`updated_at` ISO.
- Criar `src/features/cards/cardService.ts` com `createCard({ columnId, title })` e hook `useColumnCards(columnId)` para listagem — features não importam `@tauri-apps/api` fora do service.
- UI: `AddCardForm` inline no rodapé de `Column.tsx`; submit desabilitado se título vazio; novo card append ao fim com `order_index` calculado no Rust.

### Architecture refs

- `docs/07_api_contracts.md` — Cards (create_card, list_cards)
- `docs/architecture/data-model.md` — cards
- `docs/05_architecture.md` — § Modelo de dados

### API / DB impact

- INSERT em `cards`; listagem por `column_id` filtrando `archived = 0`.

### Security notes

- Rejeitar título vazio no Rust e no client.

### Related decisions

- _n/a_

### Planned

- [x] **manual** — 1. Clicar "Adicionar cartão" na coluna. 2. Tentar salvar vazio — bloqueado. 3. Criar "Revisar PR". 4. Reload — cartão visível. 5. Criar segundo cartão — aparece abaixo do primeiro.
- [x] **automated** — `pnpm lint` exit 0 em `src/features/cards/`.

## Record

### Files

- `src/features/cards/cardService.ts`
- `src-tauri/src/commands/cards.rs`

### Backend

create_card com validação título.

### Frontend

Input novo cartão por coluna.

### Scripts / Docs

_n/a_

### Executed

- pnpm build exit 0.
- **suggested commit:** `feat(cards): create card with required title (US-0009)`


## Boundaries

### Out of scope for this story

- Metadados, modal, compacto rico, DnD.

### Notes

_n/a_
