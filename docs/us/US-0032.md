---
id: US-0032
title: Migration schema agile no SQLite
epic: EPIC-11
version: v3
status: ✅
moscow: Must
depends_on: []
ready: true
done_when: "Migration aplica tabelas agile; app inicia sem erro; schema documentado em architecture."
tests: required
tests_status: done
---

# US-0032 — Migration schema agile no SQLite

**As** Usuário local,
**I want** que o banco persista projetos, versões, épicos, sprints e user stories estruturadas,
**so that** eu possa gerir desenvolvimento agile sem arquivos Markdown soltos.

## Intent

### Acceptance

- [x] Migration `YYYYMMDDHHMMSS_agile_schema.sql` cria tabelas: `projects`, `versions`, `epics`, `sprints`, `user_stories`, `acceptance_criteria`, `story_dependencies`, `workflow_columns`.
- [x] Campos de US incluem: id legível (US-XXXX), title, epic_id, version_id, sprint_id, status (❌/🔶/✅), moscow, ready, done_when, why, where_text, approach, workflow_column_id, order_index.
- [x] App Tauri inicia e aplica migration em banco novo e existente (upgrade path definido).
- [x] `docs/architecture/data-model.md` atualizado com seção agile ou arquivo `agile-data-model.md` referenciado em `05_architecture.md`.

### Why

O modelo v1 (boards/columns/cards) não expressa hierarquia produto → release → sprint → US nem critérios de aceite normalizados. Esta slice entrega apenas o schema e migration — sem UI — para que commands e telas v3 tenham fundação única. Depois dela, nenhuma US agile deve inventar tabelas ad hoc.

### Where

v3-S1 / EPIC-11. Sem dependências. Desbloqueia US-0033, US-0034 e US-0035 (API) e indiretamente toda a v3.

## Plan

### Approach

- Adicionar migration `20260702191744_agile_schema.sql` com tabelas agile e FKs compostas por `project_id`, registrada em `src-tauri/src/db/mod.rs` via `apply_migration` idempotente.
- Documentar entidades v3 em `docs/architecture/data-model.md` (§ Modelo agile) e estender tabela em `docs/05_architecture.md` — sem commands nesta slice (SRP).

### Architecture refs

- `docs/05_architecture.md` — § Modelo de dados
- `docs/architecture/data-model.md` — § Modelo agile (v3)
- `docs/06_database.md` — § Migrations

### API / DB impact

- Nova migration em `src-tauri/migrations/`; possível coexistência temporária com tabelas kanban v1.

### Security notes

- _n/a_ — dados locais single-user.

### Related decisions

- `docs/decisions/2026-07-02.json` — entrada v3 SQLite-first.

### Planned

- [x] **manual** — 1. Apagar ou usar DB limpo. 2. `pnpm tauri dev`. 3. Inspecionar schema com ferramenta SQLite. 4. Confirmar tabelas e FKs.
- [x] **automated** — `pnpm build` exit 0.

## Record

### Files

- `src-tauri/migrations/20260702191744_agile_schema.sql`
- `src-tauri/src/db/mod.rs`
- `docs/architecture/data-model.md`
- `docs/05_architecture.md`

### Backend

Migration agile + runner; teste idempotente atualizado (16 tabelas).

### Frontend

_n/a_

### Scripts / Docs

data-model + 05_architecture index agile.

### Executed

- `pnpm lint` — exit 0 (warnings only)
- `pnpm build` — exit 0
- `cargo test` — exit 0
- **suggested commit:** `feat(db): agile schema migration (US-0032)`

## Boundaries

### Out of scope for this story

- Commands Tauri CRUD — US-0033 a US-0035.
- Remover tabelas kanban v1 — decisão separada; migration pode coexistir.
- Seed de dados demo — US posterior ou manual.

### Notes

Convenção de id: `US-XXXX` com zero-padding; gerador sequencial por projeto.
