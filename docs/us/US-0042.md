---
id: US-0042
title: Modal US editar campos Meridian
epic: EPIC-14
version: v3
status: ✅
moscow: Must
depends_on: [US-0041]
ready: true
done_when: "Gestor edita Why, Where, acceptance, ready, status; validação 🔶; cartão atualiza ao salvar."
tests: required
tests_status: done
---

# US-0042 — Modal US: editar campos Meridian e checklist

**As** Usuário local,
**I want** editar a user story e seus critérios de aceite no modal,
**so that** eu refine o backlog dentro do app sem Markdown externo.

## Intent

### Acceptance

- [x] Campos editáveis: title, why, where_text, done_when, moscow, ready, status, approach (textarea), depends_on (multi-select US ids).
- [x] Acceptance: adicionar/remover linhas; toggle checked; reorder opcional out of scope.
- [x] Salvar chama `update_story` + `set_story_acceptance`; erros inline no formulário.
- [x] Status 🔶 exige texto Missing em acceptance ou campo dedicado — bloqueia save se vazio.
- [x] Status ✅ bloqueado se acceptance obrigatório incompleto (regra P0 Meridian simplificada).
- [x] Após salvar, cartão no board reflete badges (US-0039).

### Why

Visualizar (US-0041) não fecha o ciclo refine: o gestor precisa ajustar acceptance e `ready` antes de considerar a story implementável — equivalente a `/refine-us` no repositório. Esta slice completa o editor MVP da v3.

### Where

v3-S4 / EPIC-14. Depende de US-0041. Fecha epic EPIC-14 no incremento mínimo.

## Plan

### Approach

- Estender modal com modo editar: update_story, set_story_acceptance, set_story_dependencies; validação 🔶/✅ refletida do backend.
- Editor de depends_on via checklist de outras US do projeto.

### Architecture refs

- `.agent/references/templates/us-template.md` — § Intent

### API / DB impact

- Consome `update_story`, `set_story_acceptance`, `set_story_dependencies`.

### Security notes

- _n/a_

### Related decisions

- _n/a_

### Planned

- [x] **manual** — 1. Editar Why e marcar 2 acceptance. 2. Set ready true — save OK. 3. Set ✅ sem criteria — erro. 4. Set 🔶 sem Missing — erro. 5. Board atualiza.
- [x] **automated** — `pnpm lint` e `pnpm build` exit 0.

## Record

### Files

- `src/features/agile/ui/StoryDetailModal.tsx`

### Backend

_n/a_

### Frontend

Edição Meridian + acceptance + deps.

### Scripts / Docs

_n/a_

### Executed

- `pnpm lint` — exit 0 (warnings only)
- `pnpm build` — exit 0
- `cargo test` — exit 0
- **suggested commit:** `feat(agile): story edit modal (US-0042)`

## Boundaries

### Out of scope for this story

- Record / Executed automático — fluxo `/complete-us` no repo docs.
- Versionar histórico de edições — audit log futuro.
- Markdown export/import.

### Notes

`ready: true` no app ≈ gate `/refine-us` — implementação de código ainda é out-of-app ou US futura "modo dev".
