---
id: US-0067
title: Chrome inglês e visual demo projects/hub
epic: EPIC-20
version: v6
status: ✅
moscow: Must
depends_on: [US-0065]
ready: true
done_when: "Browser demo: hub/list/included em inglês; hero e card featured na lista; hub com badges e painel contexto refinado."
tests: required
tests_status: done
---

# US-0067 — Chrome inglês e visual demo projects/hub

**As** visitante internacional da demo,
**I want** English labels and a polished project entry screen,
**so that** the public demo feels intentional, not a raw dev build.

## Intent

### Acceptance

- [x] `hubCopyForRuntime()` / `includedCopyForRuntime()` — inglês quando `!isTauriRuntime()`; português no desktop.
- [x] `ProjectListPage` em demo: hero “Meridian · web demo”, card featured do sample (`DEMO_PROJECT_ID`) com pill v5+v6 e CTA.
- [x] `ProjectHubPage` em demo: classe `demo-hub-page`, badges de status coloridos, `hub-context-panel` estilizado.
- [x] CSS em `us-form.css` — responsivo, sem overflow horizontal.
- [x] `pnpm build` ok.

### Why

A demo pública misturava PT no hub com copy EN no banner; a lista de projetos era genérica demais para um showcase do repositório exemplo.

### Where

v6-S3 / EPIC-20. Depende US-0065 (banner). Não altera SQLite/Tauri strings além do guard runtime.

## Plan

### Approach

- DRY: strings hub/included em `browserDemoChrome.ts` — componentes leem `hubCopyForRuntime()` uma vez por render.
- Demo list: layout single-column featured card; gradiente de fundo sutil na page.
- Hub: status pills `.hub-panel__badge--positive|--muted`; painel inferior unificado `.hub-context-panel`.

### Architecture refs

- `docs/05_architecture.md` — § Camadas e limites

### API / DB impact

- _n/a_

### Security notes

- _n/a_

### Related decisions

- _n/a_

### Planned

- [x] **manual** — `pnpm preview`: lista hero EN, hub colunas EN, included summary EN; desktop Tauri ainda PT no hub.
- [x] **build** — `pnpm build`

## Record

### Files

- `src/lib/browserDemoChrome.ts`
- `src/features/agile/ui/ProjectHubPage.tsx`
- `src/features/agile/ui/ProjectListPage.tsx`
- `src/features/agile/ui/VersionIncludedSummary.tsx`
- `src/app/us-form.css`

### Backend

- _n/a_

### Frontend

- Chrome i18n runtime EN/PT; visual polish demo projects + hub.

### Scripts / Docs

- `docs/sprints/v6-S3.md`, US-0066/0067

### Executed

- `pnpm exec tsc --noEmit` (ok)
- `pnpm build` (ok)

## Boundaries

### Out of scope for this story

- i18n framework ou locale switch manual.
- Traduzir wizards CreateUs/EditVersion inteiros no browser.
- Reset navigation — US-0066.

### Notes

Create/edit dialogs permanecem PT até épico i18n dedicado.
