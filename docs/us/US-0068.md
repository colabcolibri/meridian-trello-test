---
id: US-0068
title: Seed demo rico v4–v6 no browser
epic: EPIC-20
version: v6
status: ✅
moscow: Must
depends_on: [US-0064]
ready: true
done_when: "Primeira carga do browser demo traz ~26 US reais do repo em v4/v5/v6, épicos e sprints ligados, quadro com cartões em todas as colunas."
tests: required
tests_status: done
---

# US-0068 — Seed demo rico v4–v6 no browser

**As** visitante da demo pública,
**I want** abrir o app já com um workspace representativo do repositório exemplo,
**so that** vejo hub cheio, quadro Meridian realista e releases v4→v6 sem digitar dados.

## Intent

### Acceptance

- [x] `createDemoSeedDb()` inclui versões **v4**, **v5**, **v6** e épicos **EPIC-18**, **EPIC-19**, **EPIC-20** com `version_epics` corretos.
- [x] ≥ **24** user stories com ids `US-0044`…`US-0067` (subset real do repo), cada uma com preamble, Why, acceptance parcial e status/coluna coerentes.
- [x] Sprints nomeados (v4-S*, v5-S*, v6-S*) e stories distribuídas em **Backlog, Refine, Ready, In progress, Review, Done** — quadro “all stories” visualmente cheio.
- [x] `loadDb()` usa storage **`meridian-agile-browser-v3`**; primeira visita persiste seed; reset (US-0066) restaura o mesmo seed.
- [x] Hero/card do project list menciona volume (ex.: “26 stories · v4–v6”).
- [x] `pnpm exec tsc --noEmit` e `pnpm build` ok.

### Why

Demo com 7 cartões não mostra o valor do produto Meridian (cartões schema v2, hub hierárquico, ciclo de workflow). Visitante precisa sentir o mesmo densidade do repo `docs/us/` ao abrir o link.

### Where

v6 / EPIC-20, após US-0064 (store). Complementa US-0067 (polish) sem duplicar chrome — foco em **dados** sample.

## Plan

### Approach

- Extrair seed para `browserDemoSeed.ts` com helper `mk()` (DRY) mapeando ids/títulos alinhados a `docs/us/*.md` — English copy no corpo da story.
- Ampliar `createDemoSeedDb()`: project description, sprints v4 e v5-S4/v6-S3, dependencies realistas entre US v5/v6.
- Bump `STORAGE_KEY` v3 para visitantes não ficarem com seed vazio antigo; manter `DEMO_PROJECT_ID` estável.
- Ajustar pill/hero em `ProjectListPage` para refletir escopo do sample.

### Architecture refs

- `docs/05_architecture.md` — § Camadas e limites
- `docs/architecture/data-model.md` — § Modelo agile

### API / DB impact

- _n/a_ — somente browser localStorage.

### Security notes

- _n/a_

### Related decisions

- _n/a_

### Planned

- [x] **manual** — `pnpm preview`: lista → hub v5 (3 versões) → open sprint v5-S3 → board com múltiplos cartões → board all stories com 6 colunas populadas.
- [x] **build** — `pnpm build`

## Record

### Files

- `src/features/agile/browser/browserDemoSeed.ts`
- `src/features/agile/browser/browserAgileStore.ts` (STORAGE_KEY v3)
- `src/features/agile/ui/ProjectListPage.tsx`

### Backend

- _n/a_

### Frontend

- Seed estático 26 US, 3 versões, 3 épicos, 10 sprints; helper `mk()`; dependencies sample.

### Scripts / Docs

- `docs/us/US-0068.md`

### Executed

- `pnpm exec tsc --noEmit` (ok)
- `pnpm build` (ok)

## Boundaries

### Out of scope for this story

- Sync seed ↔ `docs/us/*.md` automático.
- Novos ids US inexistentes no repo.
- i18n desktop Tauri.

### Notes

Seed é snapshot estático; gestor edita localmente na demo sem afetar git.
