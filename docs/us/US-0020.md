---
id: US-0020
title: README install dev e build
epic: EPIC-07
version: v1
status: ✅
moscow: Must
depends_on: [US-0018]
ready: true
done_when: "README na raiz permite novo dev instalar, rodar tauri dev e tauri build com sucesso."
tests: required
tests_status: done
---

# US-0020 — README install dev e build

**As** Usuário local,
**I want** instruções claras para instalar e usar o app,
**so that** eu consiga rodar o projeto sem adivinhação.

## Intent

### Acceptance

- [x] README descreve pré-requisitos (Node, pnpm, Rust).
- [x] Comandos: install, tauri dev, tauri build, lint/test se existirem.
- [x] Localização do arquivo SQLite e fluxo feliz resumido (criar quadro → coluna → cartão).
- [x] Gestor validou seguindo README em ambiente limpo ou documentou assumptions.

### Why

Entrega utilizável exige documentação — requisito explícito da spec. Fecha EPIC-07 e go-live checklist v1.

### Where

v1-S6 / EPIC-07. Depende de US-0018 (app funcional mínimo para documentar). Última US Must da v1.

## Plan

### Approach

- Escrever `README.md` na raiz com seções: pré-requisitos (Node 20, pnpm 9, Rust stable), instalação, `pnpm tauri dev`, `pnpm tauri build`, estrutura de pastas resumida e caminho do SQLite no app data dir por SO.
- Documentar fluxo feliz numerado (criar quadro → colunas → cartão → DnD) alinhado ao go-live checklist de `docs/versions/v1.md`.
- Adicionar troubleshooting mínimo (Rust não instalado, erro de migration) e referência a `docs/08_environments.md` — sem duplicar arquitetura completa.

### Architecture refs

- `docs/08_environments.md` — Desenvolvimento local, Produção / build local
- `docs/00_scope.md` — In scope (v1)
- `docs/versions/v1.md` — Go-live checklist

### API / DB impact

- _n/a_

### Security notes

- README não inclui secrets; menciona que `.db` é dado local do usuário.

### Related decisions

- `docs/decisions/2026-07-02.json` — Tauri toolchain

### Planned

- [x] **manual** — 1. Em pasta limpa ou máquina doc, seguir README do zero: install → tauri dev → criar quadro/coluna/cartão → tauri build. 2. Todos passos completam sem passos omitidos.
- [x] **automated** — `pnpm lint` e `pnpm build` exit 0 após doc (sanity do repo).

## Record

### Files

- `README.md`

### Backend

_n/a_

### Frontend

_n/a_

### Scripts / Docs

README com install, dev, build, caminho SQLite, fluxo feliz.

### Executed

- pnpm lint/build ok; cargo test ok; tauri build compila release (dmg opcional falhou no CI local).
- **suggested commit:** `docs: add README install dev build guide (US-0020)`


## Boundaries

### Out of scope for this story

- CI workflows, publicação em stores.

### Notes

Incluir screenshot opcional se disponível ao fechar.
